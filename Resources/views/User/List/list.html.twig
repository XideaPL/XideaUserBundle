{% extends 'XideaUserBundle::main.html.twig' %}
{% block xidea_user_header_title %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">{{ 'user_action.list.title'|trans }}</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
{% endblock %}
{% block xidea_user_main %}
    <div class="row">
        <div class="col-lg-8">
            <div class="panel panel-default">
                <div class="panel-heading">
                    {{ 'user_action.list.title'|trans }}
                </div>
                <!-- /.panel-heading -->
                <div class="panel-body">
                    <div class="table-responsive">
                        <table class="table table-striped">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>{{ 'user.name'|trans }}</th>
                                    <th>{{ 'user.created_at'|trans }}</th>
                                    <th>{{ 'user.updated_at'|trans }}</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for model in models %}
                                    <tr>
                                        <td>{{ model.id }}</td>
                                        <td>{{ model.name }}</td>
                                        <td>{{ model.createdAt|date() }}</td>
                                        <td>{{ model.updatedAt|date() }}</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    <!-- /.table-responsive -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
    </div>
{% endblock %}
