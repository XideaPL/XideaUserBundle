{% extends xidea_template('xidea_user', 'user_main') %}
{% block bundle_title %}
    <h1 class="page-header">{{ 'user_list.title'|trans }}</h1>
{% endblock %}
{% block bundle_main %}
    {% block user_main %}
        <div class="box box-default">
            <div class="box-header">
                <h3 class="box-title">{{ 'user_list.title'|trans }}</h3>
            </div>
            <!-- /.box-header -->
            <div class="box-body">
                {% block user_list %}
                    <div class="user-list">
                        <table class="table table-striped user-table">
                            <thead>
                                <tr>
                                    <th>#</th>
                                    <th>{{ 'user.name'|trans }}</th>
                                    <th>{{ 'user.created_at'|trans }}</th>
                                    <th>{{ 'user.updated_at'|trans }}</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for model in models %}
                                    <tr>
                                        <td>{{ model.id }}</td>
                                        <td>{{ model.name }}</td>
                                        <td>{{ model.createdAt|date() }}</td>
                                        <td>{{ model.updatedAt|date() }}</td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                            <tfoot>
                                <tr>
                                    <th>#</th>
                                    <th>{{ 'user.name'|trans }}</th>
                                    <th>{{ 'user.created_at'|trans }}</th>
                                    <th>{{ 'user.updated_at'|trans }}</th>
                                </tr>
                            </tfoot>
                        </table>
                    </div>
                    <!-- /.user-list -->
                {% endblock %}
            </div>
            <!-- /.box-body -->
        </div>
        <!-- /.box -->
    {% endblock %}
{% endblock %}
